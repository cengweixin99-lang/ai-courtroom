# 前端先在宿主机完成类型检查和生产构建，再复用现有 Nginx 运行时。
FROM mootcourt-lab-web:local-runtime

COPY nginx.conf /etc/nginx/conf.d/default.conf
# 清理旧哈希资源，确保容器中只保留本次生产构建产物。
RUN rm -rf /usr/share/nginx/html/*
COPY dist /usr/share/nginx/html
