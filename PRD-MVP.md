# 模拟法庭多 Agent 系统 PRD（MVP）

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品暂定名 | MootCourt Lab / 模拟庭审实验室 |
| 文档版本 | v0.2 |
| 产品阶段 | MVP |
| 目标用户 | 法学生、模拟法庭参与者、法律通识学习者 |
| 产品定位 | 基于虚构案卷、真实且版本化法律依据的教学型模拟庭审与复盘工具 |
| 非产品定位 | 真实案件裁判、法律咨询、律师替代工具 |
| MVP 法域 | 中华人民共和国刑事一审教学场景 |
| MVP 案件类型 | 单一虚构盗窃案件 |
| 法律基准日 | 2026-07-14；实际入库前须根据官方法源再次核验 |

## 2. 产品概述

MootCourt Lab 是一个用于法学教学和庭审训练的多角色 Agent 系统。用户在虚构案件中选择公诉方或辩护方参与庭审，系统使用公诉方、辩护方和“被告人/证人陈述执行器”三类角色执行器补齐其他角色，并由确定性庭审控制器管理程序阶段、证据权限、发言权和程序性请求。用户选择的一方由用户本人承担，因此一场庭审实际只调用另外两类 AI 角色执行器。

MVP 的核心不是生成逼真的法庭对话，而是验证以下 Agent 工程能力：

- 不同角色只能获得其有权访问的信息；
- 所有事实主张能够追溯到案卷证据；
- 非法阶段操作和不存在的证据能够被程序拒绝；
- 证人能够维持知识边界和陈述一致性；
- 系统能够记录执行轨迹、成本和可重复评估指标。

## 3. 背景与问题

传统模拟法庭训练通常要求多名参与者同时到场，还依赖教师或专业人员承担主持、角色补位和复盘工作。个人练习时，学习者难以获得完整的庭审互动，也难以系统检查自己的主张是否有证据支持、询问是否有效、是否忽略了关键矛盾。

通用聊天模型虽然可以扮演法律角色，但存在明显限制：

- 容易引用案卷中不存在的事实；
- 不同角色可能获得不应知道的信息；
- 无法稳定遵循庭审阶段和发言规则；
- 角色观点、客观事实和模型推断容易混淆；
- 缺少可追溯记录和可重复评估。

本产品通过“确定性程序控制 + 按角色构造上下文 + 结构化 Agent 输出 + 证据校验”解决上述问题。

## 4. 产品目标

### 4.1 MVP 目标

1. 支持用户基于一个虚构案件完成一次 20 至 30 分钟的简化模拟庭审。
2. 支持用户选择公诉方或辩护方，AI 自动补齐另一方以及被告人、证人角色；用户所选一方不再调用同角色 Agent。
3. 使用状态机控制庭审阶段和合法动作，不由模型自由推进流程。
4. 对 Agent 产生的事实主张执行证据 ID、角色权限和阶段合法性校验；用户陈述不被阻止，但无证据主张会被标记并进入复盘。
5. 庭审结束后生成基于记录和证据的教学复盘。
6. 保存模型调用、工具调用、错误、Token、延迟和成本数据。
7. 建立不少于 50 条自动化 Eval 样例，分别覆盖程序与权限、角色知识边界、法律 RAG 和端到端庭审。
8. 结案分析必须根据锁定法域中真实、可核验、带生效版本的法律依据完成，不得由模型凭常识自由裁判。

### 4.2 成功标准

MVP 达到以下条件视为完成：

- 用户能够从开庭陈述完整进行到结案复盘；
- 不存在证据引用能够被系统拦截；
- 未授权信息不会进入对应角色的模型上下文；
- 证人回答能够追溯到其允许事实或历史陈述；
- 非法阶段操作能够被确定性规则拒绝；
- 一场庭审可以重复运行并生成完整 trace；
- Eval 报告能够展示可靠性、成本和延迟数据。
- 结案报告能够逐项展示教学模拟事实判断、构成要件、证明标准、法律依据和推导结果。

## 5. 非目标

MVP 明确不包含：

- 真实案件分析或法律建议；
- 对现实个人或机构作出有罪、无罪或责任判断；
- 自动生成完整案卷；
- 中国法、英美法等多法域切换；
- 对中华人民共和国刑事诉讼全部程序的完整复刻；
- 多名常驻陪审员 Agent；
- 语音识别、语音合成和虚拟人；
- 3D 法庭、动画或复杂游戏化系统；
- 多名真人实时联机；
- 本案适用范围以外的大型、通用法律知识库；
- 自动发布、分享或提交庭审结果。

“不完整复刻现实程序”仅指 MVP 对庭审交互步骤做教学化裁剪，不代表使用虚构实体法。实体法依据、证明责任、证明标准及关键证据规则必须来自项目明确选定的真实法域。

## 6. 用户与使用场景

### 6.1 目标用户

**法学生**

希望独立练习法庭调查、举证质证、法庭辩论和最后陈述，并获得结构化复盘。

**模拟法庭参与者**

希望在正式训练前反复测试论证和提问策略。

**法律通识学习者**

希望通过一个短案件理解证据、事实主张和庭审程序之间的关系。

### 6.2 核心用户故事

- 作为用户，我可以阅读自己有权查看的训练材料，并选择公诉方或辩护方。
- 作为用户，我可以在合法阶段发表意见、出示证据、询问证人或提出程序性请求。
- 作为用户，我可以看到当前阶段、当前发言人、已提交证据和剩余动作。
- 作为用户，我希望 AI 角色不能使用案卷之外或其无权访问的信息，同时我的无证据观点会得到提示而不会被系统代写。
- 作为用户，我希望庭审结束后看到自己的主张、证据使用、有效询问和遗漏问题。
- 作为教师或项目评估者，我可以查看完整 trace 和 Eval 结果。

## 7. MVP 使用流程

1. 用户进入案件大厅，选择唯一的 MVP 虚构案件。
2. 系统展示教学免责声明、案件简介和预计时长。
3. 用户选择公诉方或辩护方。
4. 系统根据角色加载公共材料和角色私有材料。
5. 庭审控制器依次推进：
   - 开庭说明；
   - 宣读起诉书与被告人陈述；
   - 法庭调查；
   - 公诉方、辩护方举证质证；
   - 证人作证及各方发问；
   - 法庭辩论；
   - 被告人最后陈述；
   - 教学复盘。
6. 每个阶段只显示当前合法动作。
7. 用户或 Agent 提交动作后，系统先校验，再写入公开庭审记录。
8. 庭审结束后，系统生成证据使用、主张支持、程序错误和改进建议报告。

## 8. 角色与职责

### 8.1 公诉方 Agent

- 基于公诉方材料建立指控论证；
- 选择和提交证据；
- 询问证人；
- 回应辩护方质疑；
- 完成公诉意见和法庭辩论发言；
- 每项事实主张必须提供证据 ID。

### 8.2 辩护方 Agent

- 检查公诉方证据缺口；
- 提出替代解释；
- 对证人发问并对证据发表质证意见；
- 使用辩护方材料建立合理质疑；
- 完成辩护意见和法庭辩论发言；
- 不得将推断表述为已确认事实。

### 8.3 被告人/证人陈述执行器

- 根据当前 `participant_id` 和 `participant_type` 加载被告人或证人档案；
- 被告人陈述与证人证言分别维护，不将被告人错误建模为证人；
- 只根据角色亲历事实、案卷中的既有陈述和本庭历史回答；
- 对不确定或未知内容明确表示不确定或不知道；
- 保持同一角色在本庭中的陈述历史，并标记与既有陈述的差异；
- 不得访问完整案件真相、其他证人私有材料或公诉方、辩护方策略。

### 8.4 庭审控制器

庭审控制器是确定性程序模块，不是自由 Agent，负责：

- 当前阶段和当前发言权；
- 当前阶段允许的动作；
- 证据提交和采纳状态；
- 角色信息权限；
- 证人出庭状态；
- 程序性请求与处理流程；
- 回合、Token、时间和成本预算；
- 庭审结束条件。

### 8.5 程序处理解释节点

- 仅在程序性请求存在语义模糊时调用；
- 规则引擎先完成硬性校验；
- LLM 只生成教学性处理理由；
- 不负责推进阶段或直接修改案件状态。

### 8.6 结案法律适用模块

结案法律适用模块不作为自由裁判 Agent，而是“确定性要件检查 + 法律 RAG + 引用校验 + 结构化分析 + 人工审核”的组合模块，负责：

- 锁定案件适用的法域、案件类型和法律生效日期；
- 根据争议事项、案由和构成要件，通过法律 RAG 从审核后的法源库检索候选法条、司法解释及必要的证据规则；
- 读取本案经专业人员预置并审核的法定构成要件清单，RAG 不得自行增删必要要件；
- 对每个构成要件分别列出已支持事实、有争议事实、支持证据、反驳证据和证明状态；
- 应用该法域对应的举证责任与证明标准；
- 生成“满足、未满足、证据不足或不适用”的逐项结论；
- 为每一项法律结论附带可点击、可核验的法律来源和版本信息；
- 在法域、法律版本、必要事实或证据不足时拒绝生成确定性结论。

LLM 可以生成解释，但不得自行创建法条、改变构成要件或绕过证明标准。MVP 输出属于教学模拟结论，不构成现实裁判或法律意见。

法律 RAG 仅负责检索和提供证据，不直接决定结论。任何召回内容在进入结案分析前，都必须通过法域、生效日期、效力状态、条款编号和官方来源校验。

## 9. 功能需求

### 9.1 案卷与角色权限

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-001 | 加载案件摘要、争议事实、证据和证人数据 | P0 |
| FR-002 | 区分公共材料、公诉方材料、辩护方材料、被告人材料和证人材料 | P0 |
| FR-003 | 根据当前角色动态构造可见上下文 | P0 |
| FR-004 | 未授权数据不得发送给模型 | P0 |
| FR-005 | 展示用户当前有权查看的案卷内容 | P0 |

### 9.2 庭审状态机

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-010 | 按预定义顺序推进庭审阶段 | P0 |
| FR-011 | 每个阶段限定合法发言人和动作 | P0 |
| FR-012 | 支持结束询问和推进阶段 | P0 |
| FR-013 | 非法阶段操作返回明确错误，不调用模型 | P0 |
| FR-014 | 显示当前阶段、发言人和剩余回合 | P0 |

### 9.3 证据和事实主张

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-020 | 支持按证据 ID 提交证据 | P0 |
| FR-021 | 检查证据是否存在及角色是否有权使用 | P0 |
| FR-022 | 跟踪证据未提交、待处理和已提交状态 | P0 |
| FR-023 | 公诉方、辩护方主张关联证据 ID 或标记为推断；被告人、证人陈述关联既有陈述 ID、亲历事实 ID 或标记为本庭新增陈述 | P0 |
| FR-024 | 不存在或无权使用的证据引用不得进入公开记录 | P0 |
| FR-025 | 维护事实、支持证据和反驳证据之间的关系 | P1 |

### 9.4 发问、质证和程序性请求

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-030 | 支持公诉方、辩护方向证人发问 | P0 |
| FR-031 | 被告人或证人回答必须关联允许事实、既有陈述或明确标记为本庭新增陈述 | P0 |
| FR-032 | 支持制止与案件无关、重复、诱导或要求证人猜测的问题 | P0 |
| FR-033 | 支持对证据的真实性、合法性、关联性和证明力发表质证意见 | P0 |
| FR-034 | 模糊程序性请求可调用程序处理解释节点 | P1 |
| FR-035 | 程序性请求、处理结果及质证意见写入庭审记录 | P0 |

### 9.5 复盘与可观测性

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-040 | 保存完整公开庭审记录 | P0 |
| FR-041 | 保存模型调用、工具调用、校验失败和修复记录 | P0 |
| FR-042 | 统计每场 Token、估算成本和响应时间 | P0 |
| FR-043 | 生成证据使用和主张支持情况 | P0 |
| FR-044 | 指出未被证据支持的主张和遗漏争议点 | P1 |
| FR-045 | 生成教学性改进建议 | P1 |

### 9.6 结案与法律适用

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-050 | 案件启动前锁定法域、案件类型和法律生效日期 | P0 |
| FR-051 | 指控或争议事项映射到人工审核并版本化的法定构成要件清单 | P0 |
| FR-052 | 每个构成要件关联已支持事实、有争议事实、证据和证明状态 | P0 |
| FR-053 | 根据选定法域应用举证责任和证明标准 | P0 |
| FR-054 | 每项法律结论必须引用真实法源及具体条款 | P0 |
| FR-055 | 法源不存在、版本不明或必要事实不足时禁止生成确定性结论 | P0 |
| FR-056 | 区分教学模拟事实判断、法律适用、教学解释和模型推断 | P0 |
| FR-057 | 结案结果支持人工审核和修正，并保留审计记录 | P1 |

### 9.7 法律 RAG

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR-060 | 从审核后的官方法源构建版本化法律知识库 | P0 |
| FR-061 | 根据法域、案件类型和基准日期先执行元数据过滤 | P0 |
| FR-062 | 同时支持条款编号/关键词检索与向量语义检索 | P0 |
| FR-063 | 对混合检索结果进行重排并保留检索分数 | P1 |
| FR-064 | 返回法条原文、上下文条款、版本及官方来源 | P0 |
| FR-065 | 对候选法源执行法域、生效期、效力状态和条款真实性校验 | P0 |
| FR-066 | 检索置信度不足或结果冲突时停止自动结论并请求人工审核 | P0 |
| FR-067 | 保存查询、过滤条件、召回结果、重排结果和最终引用 | P0 |
| FR-068 | 支持按构成要件分别检索，避免使用整份庭审记录进行宽泛查询 | P0 |

## 10. 庭审规则与真实法律依据

MVP 的庭审交互使用经过明确标注的教学化简化程序，但结案必须依据项目锁定法域中的真实法律。项目启动时必须在 `LEGAL_PROFILE` 中固定：

- 法域；
- 案件类型；
- 法律适用基准日期；
- 实体法来源；
- 程序法来源；
- 证据规则来源；
- 举证责任与证明标准；
- 已被简化或省略的程序及其教学说明。

首版不得混用英美对抗制异议规则或其他法域概念。庭审阶段、术语、举证责任、证明标准和结案结构必须按中华人民共和国现行法律及教学场景审核。

### 10.1 阶段

1. `COURT_OPENING`
2. `INDICTMENT_AND_DEFENDANT_STATEMENT`
3. `COURT_INVESTIGATION`
4. `PROSECUTION_EVIDENCE_AND_EXAMINATION`
5. `DEFENSE_EVIDENCE_AND_EXAMINATION`
6. `WITNESS_QUESTIONING`
7. `COURT_DEBATE_PROSECUTION`
8. `COURT_DEBATE_DEFENSE`
9. `DEFENDANT_FINAL_STATEMENT`
10. `LEGAL_ANALYSIS`
11. `REVIEW`
12. `COMPLETED`

### 10.2 MVP 程序性请求与质证类型

- `IRRELEVANT_QUESTION`：请求制止与案件事实无关的问题；
- `REPETITIVE_QUESTION`：请求制止已经得到回答的重复问题；
- `IMPROPER_QUESTION`：请求制止诱导、威胁、侮辱或要求证人无依据猜测的问题；
- `EVIDENCE_CHALLENGE`：针对证据真实性、合法性、关联性或证明力提出质证意见。

上述分类是教学系统中的结构化操作入口，不等同于英美法庭的 objection。具体名称、处理规则和法律依据必须在案卷发布前由相关专业人员审核。

### 10.3 结案推导顺序

结案模块必须按以下顺序运行：

1. 确认适用法域、案由和法律版本；
2. 列出待证明事项及法定构成要件；
3. 对庭审中出现的证据进行合法性、相关性和证明力标记；
4. 形成逐项教学模拟事实判断，状态仅限“已支持、有争议、证据不足”，不以案卷隐藏真相直接替代庭审证明；
5. 对每个构成要件应用举证责任和证明标准；
6. 仅根据已认定事实进行法律适用；
7. 输出支持结论、反对结论、未解决争点和不确定性；
8. 附上法条、版本、来源链接及教学免责声明。

### 10.4 法律 RAG 检索流程

```text
案由与争议事项
→ 读取人工审核的构成要件/程序问题清单
→ 生成逐项法律查询
→ LegalProfile 元数据过滤
→ BM25/关键词与向量混合检索
→ 条款级重排
→ 相邻条款与引用关系扩展
→ 生效日期、法域、效力层级校验
→ 引用片段组装
→ 法律适用分析
→ 引用一致性复核
```

检索查询必须来自结构化的争议事项或经审核的构成要件，而不是把完整庭审记录直接作为查询。一个查询只处理一个法律问题，例如“盗窃罪非法占有目的构成要件”或“证人证言证明力审查”。

MVP 采用混合检索：

- 精确检索用于法律名称、条款编号、法定术语和固定表达；
- 向量检索用于自然语言案情与法律概念之间的语义匹配；
- 元数据过滤用于排除错误法域、错误案件类型和不适用于基准日期的版本；
- 重排模型用于在小规模候选集中提高相关条款排序；
- 引用图扩展用于补充被当前条款引用的定义条款、例外条款和关联司法解释。

不得只依赖向量相似度召回法律依据。法律条文中的否定、例外、但书和生效时间可能决定结论，必须保留完整条文及必要的相邻上下文。

## 11. 数据需求

### 11.1 单个 MVP 案件规模

- 1 个完全虚构案件；
- 1 条客观时间线；
- 10 至 15 个事实节点；
- 8 至 12 项证据；
- 1 名被告人及 2 至 3 名证人；
- 公诉方、辩护方各 1 份私有策略材料；
- 每项证据关联支持或反驳的事实；
- 被告人和每名证人分别定义亲历事实、既有陈述、未知事实和不确定内容；
- 50 条以上 Eval 样例，且四个测试子集均达到最低样例数。

### 11.2 核心实体

**Case**

- `id`
- `title`
- `summary`
- `disputed_issue_ids`
- `public_material_ids`
- `ground_truth_timeline`

**Evidence**

- `id`
- `type`
- `title`
- `content`
- `source`
- `reliability_notes`
- `available_to`
- `related_fact_ids`
- `status`

**Fact**

- `id`
- `description`
- `status`
- `supporting_evidence_ids`
- `contradicting_evidence_ids`

**Witness**

- `id`
- `public_profile`
- `private_background`
- `allowed_fact_ids`
- `statement_ids`
- `uncertainties`
- `forbidden_fact_ids`

**Defendant**

- `id`
- `public_profile`
- `known_fact_ids`
- `prior_statement_ids`
- `defense_position`
- `uncertainties`
- `forbidden_fact_ids`

**Claim**

- `id`
- `speaker`
- `text`
- `claim_type`
- `evidence_ids`
- `validation_status`

**LegalSource**

- `id`
- `jurisdiction`
- `instrument_title`
- `article_number`
- `effective_from`
- `effective_to`
- `official_source_url`
- `text_snapshot`
- `review_status`
- `source_type`
- `authority_level`
- `status`
- `version_hash`
- `parent_article_id`
- `referenced_source_ids`
- `embedding_version`
- `indexed_at`

**LegalChunk**

- `id`
- `legal_source_id`
- `chunk_type`
- `article_number`
- `text`
- `context_before`
- `context_after`
- `keywords`
- `embedding`
- `effective_from`
- `effective_to`
- `jurisdiction`
- `review_status`

法律文本优先按“法律文件 → 编/章/节 → 条 → 款/项”结构切分。不得使用固定字符长度切分破坏条文层级；一个检索结果必须能够回溯到完整条款和所属法律文件。

**LegalElement**

- `id`
- `issue_id`
- `description`
- `legal_source_ids`
- `burden_holder`
- `proof_standard`
- `supporting_fact_ids`
- `status`
- `review_status`
- `version`

**LegalProfile**

- `jurisdiction`
- `case_type`
- `law_as_of_date`
- `substantive_law_source_ids`
- `procedural_law_source_ids`
- `evidence_rule_source_ids`
- `simplifications`
- `reviewed_by`
- `reviewed_at`

### 11.3 法律 RAG 数据准备

MVP 不开发通用法律爬虫。首案法源由人工从官方渠道收集、核验和导入，并生成 `source_manifest.json`。至少准备：

- 与首案罪名直接相关的实体法条文及必要的上下文条款；
- 与刑事一审教学流程、举证责任和证明标准相关的程序法条文；
- 首案确有必要的现行司法解释；
- 每份法源的官方 URL、标题、发布机关、效力状态、生效日期和版本哈希；
- 本案人工审核的构成要件清单及其法源映射；
- 至少 20 条法律检索标注查询，包含目标法源和不可接受的干扰法源。

法源导入工具只接受 `review_status=approved` 的清单项进入生产索引。网页抓取失败、正文缺失或版本无法核验时，该法源不得进入可用于结案的索引。

## 12. Agent 输出协议

### 12.1 公诉方/辩护方输出

公诉方和辩护方使用同一个输出 Schema，通过 `speaker_role` 区分角色。允许值仅为 `prosecution`（公诉方）和 `defense`（辩护方）。

```json
{
  "speaker_role": "prosecution",
  "speech": "根据证据 E03……",
  "claims": [
    {
      "text": "有人于 21:10 进入现场",
      "claim_type": "supported_fact",
      "evidence_ids": ["E03"]
    }
  ],
  "requested_action": "question_witness",
  "target_id": "W01"
}
```

### 12.2 证人输出

```json
{
  "answer": "我看到一名穿深色外套的人进入现场。",
  "supported_by_statement_ids": ["W01-S02"],
  "certainty": "medium",
  "refused_reason": null
}
```

### 12.3 被告人输出

```json
{
  "answer": "我当晚去过现场，但没有拿走涉案物品。",
  "supported_by_statement_ids": ["D01-S01"],
  "new_statement": false,
  "certainty": "high",
  "refused_reason": null
}
```

### 12.4 结案法律分析输出

```json
{
  "legal_profile_id": "CN-CRIMINAL-THEFT-2026-07-14",
  "issues": [
    {
      "issue_id": "LI-01",
      "element_id": "LE-01",
      "element_text": "经审核的构成要件文本",
      "fact_status": "disputed",
      "supporting_fact_ids": ["F02"],
      "contradicting_fact_ids": ["F07"],
      "evidence_ids": ["E03", "E07"],
      "legal_source_ids": ["LS-01"],
      "proof_status": "insufficient",
      "reasoning_summary": "现有证据不足以排除相关合理疑点。"
    }
  ],
  "overall_status": "insufficient_evidence",
  "unresolved_issues": ["证人辨认可靠性仍有争议"],
  "disclaimer": "仅为虚构案件的教学模拟分析，不构成现实裁判或法律意见。"
}
```

允许的 `fact_status` 仅为 `supported`、`disputed`、`insufficient`；允许的 `proof_status` 仅为 `met`、`not_met`、`insufficient`、`not_applicable`。结案节点不得自由生成新的构成要件 ID 或法律来源 ID。

## 13. 页面需求

### 13.1 案件选择页

- MVP 案件标题与摘要；
- 教学用途免责声明；
- 预计时长；
- 公诉方或辩护方选择；
- 开始庭审按钮。

### 13.2 庭审工作台

- 顶部：阶段、当前发言人、回合和预算；
- 左侧：用户可见案卷、证据和证人；
- 中间：公开庭审记录与当前发言；
- 右侧：争议事实、已提交证据和证人状态；
- 底部：当前合法动作，如发言、出示证据、发问、质证和结束当前环节。

### 13.3 复盘页

- 庭审阶段时间线；
- 主要主张与证据关系；
- 无证据或证据不足的主张；
- 有效询问和被忽略的矛盾；
- 程序违规和系统拦截记录；
- Token、成本和延迟；
- 教学改进建议。

复盘页另设“法律适用”区域，按以下结构展示：

- 适用法域和法律基准日期；
- 待证明事项；
- 逐项构成要件；
- 每项要件对应的已支持事实、有争议事实、证据不足事项及证据；
- 举证责任和证明标准；
- 满足、未满足、证据不足或不适用的结论；
- 法条原文、条款编号、版本和官方来源；
- 仍存在的事实争议和法律不确定性。

## 14. 非功能需求

### 14.1 可靠性

- 所有 Agent 输出必须通过 Pydantic Schema；
- 格式失败最多自动修复一次；
- 无效证据、越权数据和非法阶段动作由程序拒绝；
- 每场庭审设置最大回合、最大 Token、最长时间和最高成本；
- 失败时保留可诊断 trace，不伪造成功结果。

### 14.2 性能与成本

- 每轮只调用当前需要发言的角色；
- 固定案卷内容使用缓存友好的稳定前缀；
- 只加载当前角色有权访问且与阶段相关的信息；
- 阶段历史压缩为结构化摘要；
- 单次常规发言目标响应时间小于 10 秒；
- MVP 上线前根据实际模型价格设定单场成本预算。

### 14.3 安全与合规

- 所有案件必须为虚构或获得明确授权；
- 页面持续标明教学用途，不提供现实法律建议；
- 不接收或展示真实案件当事人的敏感资料；
- 案卷内容视为不可信数据，不能覆盖系统指令；
- 不自动向外部平台发送庭审内容。
- 法律知识只能来自审核后的法源数据；模型生成内容不得直接写入法源库；
- 法律修订后旧版本不得被静默覆盖，历史案件必须保留其适用版本；
- 未完成法域配置和专业审核的案件只能运行流程测试，不得输出法律结论。

### 14.4 法律 RAG 可靠性

- 只索引审核通过的官方法源快照；
- 每条索引记录必须包含法域、生效期、效力状态、官方 URL 和版本哈希；
- 法律更新采用新增版本，不覆盖旧版本；
- 检索前强制应用 `LegalProfile` 和 `law_as_of_date` 过滤；
- 最终引用必须与检索到的原文逐字核对，模型不得改写后冒充原文；
- 检索不到足够依据时返回 `INSUFFICIENT_LEGAL_AUTHORITY`；
- 多个有效法源冲突或效力关系不明确时进入人工审核；
- 案卷、用户输入和检索文档中的指令性文本均视为不可信内容。

## 15. Eval 方案

### 15.1 硬指标

| 指标 | 定义 | MVP 目标 |
| --- | --- | --- |
| 证据引用准确率 | 引用存在且内容与主张相关 | >= 95% |
| 不存在证据拦截率 | 虚构证据引用被程序阻止 | 100% |
| 未授权信息泄漏率 | 角色输出包含未授权事实 | 0% |
| 非法阶段操作拦截率 | 不合法动作被程序拒绝 | 100% |
| 角色知识边界合规率 | 被告人/证人回答可追溯到允许事实、既有陈述或明确拒答 | >= 95% |
| Schema 首次通过率 | 无需修复即通过结构校验 | >= 90% |
| 庭审完整完成率 | 无人工修复完成全流程 | >= 80% |
| 法条引用真实性 | 条款、文本、版本和官方来源均可核验 | 100% |
| 构成要件覆盖率 | 结案分析覆盖案件配置的全部法定要件 | 100% |
| 事实到法律可追溯率 | 法律结论可追溯到认定事实、证据和法源 | 100% |
| 跨法域规则混用次数 | 使用不属于当前 LegalProfile 的规则 | 0 |
| RAG Recall@5 | 人工标注的必要法源是否进入前 5 条候选 | >= 90% |
| RAG Precision@5 | 前 5 条候选中与当前法律问题相关的比例 | >= 70% |
| 有效期过滤准确率 | 是否正确排除基准日期不适用的版本 | 100% |
| 引用忠实度 | 结论引用是否由召回原文直接支持 | >= 95% |
| 拒答准确率 | 缺少充分法源时是否停止确定性结论 | >= 95% |

### 15.2 发布门槛

以下条件是教学试玩的阻断项，不以平均分抵消：

- 法域、教学流程、案卷、待证明事项和构成要件已由至少一名具备中国刑事法学习或实务背景的审核者签字确认；
- 首案所用法源全部来自可核验的官方来源，并冻结版本哈希；
- 不存在证据拦截率、未授权信息泄漏率、非法阶段操作拦截率、法条引用真实性、有效期过滤准确率和跨法域规则混用六项必须达到表中目标；
- 所有 Eval 原始样例、预期结果、实际结果和失败 trace 可复现；
- 系统清晰展示教学用途及能力边界，不将输出命名为现实判决书或法律意见书；
- 任一阻断项未通过时，系统只能标记为 `TECH_DEMO`，不得开放为教学产品。

### 15.3 成本指标

- 单场模型调用次数；
- 单场输入和输出 Token；
- 单场估算成本；
- 每阶段平均响应时间；
- 格式修复和重试占比。

### 15.4 Eval 样例类型

最小测试集划分：

- 程序、证据和权限测试不少于 15 条；
- 被告人/证人知识边界与陈述一致性测试不少于 10 条；
- 法律 RAG 检索、版本过滤和引用忠实度测试不少于 20 条；
- 完整庭审端到端测试不少于 5 条。

- 引用不存在的证据；
- 使用对方私有材料；
- 被告人或证人回答其不知道的事实；
- 错误阶段提交证据；
- 重复提问；
- 要求证人无依据猜测；
- 将推断写成确定事实；
- 案卷文本包含提示注入；
- 长庭审历史导致角色遗忘；
- 模型输出不符合 Schema。
- 引用已失效或不适用于基准日期的法条；
- 遗漏必要构成要件；
- 使用隐藏真相代替庭审证据作教学模拟事实判断；
- 混用其他法域的规则或证明标准。
- 同一条文的新旧版本同时召回但未正确选择；
- 只召回相关概念说明，遗漏决定结论的但书或例外；
- 语义相似但不属于当前案由或法域的条文；
- 检索结果不足时模型凭参数记忆补写法条；

## 16. 技术方案

| 模块 | 建议技术 |
| --- | --- |
| Web 前端 | React + TypeScript + Vite |
| API 服务 | FastAPI + Python |
| Agent 编排 | LangGraph |
| 数据模型 | Pydantic |
| 业务数据存储 | MySQL + SQLAlchemy |
| 法律检索 | Elasticsearch 8.19.10 单后端（BM25 + 向量 + 元数据过滤） |
| Embedding | 支持中文法律文本的嵌入模型，版本固定并记录 |
| Reranker | P1：支持中文的交叉编码重排模型或受控 API |
| 实时响应 | Server-Sent Events |
| 测试 | pytest + Vitest |
| 部署 | Docker Compose |
| 可观测性 | 结构化 trace 表，可选 LangSmith |

Redis、任务队列和 WebSocket 不进入首版。法律 RAG 属于 P0，统一使用 Elasticsearch 8.19.10 完成 BM25、向量检索和元数据过滤；不再引入 PostgreSQL/pgvector，避免同时维护多套数据库。若本地资源不足，可在开发阶段先用 Elasticsearch BM25 + 元数据过滤建立基线，再启用同一后端的向量索引。Kibana 仅用于索引检查和检索调试，不作为 API 运行时依赖。

## 17. 里程碑（7 周）

### M0：法律与案卷设计冻结（第 1 周）

- 由具备相关背景的审核者确认中国刑事一审教学流程；
- 冻结单一虚构盗窃案、待证明事项和构成要件清单；
- 确认首案所需官方法源、法律版本和证明标准；
- 完成至少 10 条庭审规则和 20 条法律检索的人工标注样例。

### M1：确定性庭审引擎（第 2 周）

- 完成案件、证据、事实、证人 Schema；
- 完成唯一虚构案卷；
- 完成阶段状态机和权限规则；
- 使用固定文本走通完整庭审。

### M2：角色 Agent（第 3 周）

- 接入公诉方、辩护方和被告人/证人陈述执行节点；
- 实现角色上下文隔离；
- 实现结构化输出与一次修复。

### M3：法律 RAG（第 4 周）

- 导入首案需要的真实官方法源及版本元数据；
- 完成按条款层级切分、索引和版本管理；
- 实现元数据过滤、BM25 + 向量混合检索和引用校验；
- 建立首批法律检索标注集；
- 检索不足时能够停止结论并转人工审核。

### M4：证据、质证与可靠性（第 5 周）

- 完成证据引用和权限验证；
- 完成四类程序性请求和质证入口；
- 完成被告人/证人知识边界与一致性记录；
- 加入回合、Token、时间和成本限制。

### M5：Eval（第 6 周）

- 完成 50 条以上 Eval，并满足四个测试子集的最低数量；
- 生成可靠性和成本报告；
- 修复主要失败类型。

### M6：前端与交付（第 7 周）

- 完成案件页、庭审工作台和复盘页；
- 完成 Docker Compose；
- 完成 README、架构图和演示视频；
- 邀请至少 3 名用户完成试玩并记录反馈。

## 18. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 法律程序不准确 | 降低可信度 | 明确简化教学规则，邀请法学生审核 |
| 法律版本错误或法条失效 | 产生错误结案分析 | 法源版本化、固定适用日期、只使用官方来源并进行专业审核 |
| 混用不同法域规则 | 推导失真 | 每个案件强制绑定唯一 LegalProfile，跨法域规则在执行层拒绝 |
| 模型跳过构成要件直接下结论 | 结案不可解释 | 使用确定性要件清单，未完成逐项检查不得进入结论节点 |
| RAG 召回错误或遗漏关键例外 | 法律适用错误 | 混合检索、相邻条款扩展、重排和人工标注 Recall Eval |
| 法源采集不完整或来源不权威 | 引用不可核验 | 仅导入官方来源，记录快照、版本哈希和审核状态 |
| Agent 编造证据 | 破坏核心体验 | 证据 ID、权限和内容由程序校验 |
| 被告人或证人泄露真相 | 失去公平性 | 未授权事实不进入对应角色上下文 |
| 多 Agent 成本过高 | 无法持续测试 | 每轮只调用当前角色，限制输出与回合 |
| 模型辩论冗长 | 节奏失控 | 每阶段设置字数、动作和回合限制 |
| 范围持续扩大 | 无法按期交付 | 严格执行非目标清单和七周里程碑 |
| Eval 缺少专业标准 | 指标失真 | 先评硬约束，软质量由法学生抽样审核 |

## 19. 上线前检查清单

- [ ] 案件全部为虚构内容且无真实敏感信息；
- [ ] 每条证据具有唯一 ID 和角色权限；
- [ ] 被告人和每名证人均定义允许和禁止事实；
- [ ] 固定文本能够走完整个状态机；
- [ ] 所有 Agent 输出通过 Schema 校验；
- [ ] 不存在证据和未授权信息测试通过；
- [ ] 单场成本和最大预算已配置；
- [ ] 教学免责声明在开始页和复盘页可见；
- [ ] 50 条以上 Eval 可以重复执行，且四个测试子集均满足最低数量；
- [ ] 至少完成一次真实用户试玩和问题复盘。
- [ ] 法域、案卷、构成要件和教学流程已完成专业审核并记录审核版本；
- [ ] 法源快照、官方链接和版本哈希已冻结；
- [ ] 第 15.2 节全部发布门槛通过。

## 20. 待冻结决策

以下事项必须在 M0 结束前形成书面决定，之后变更需要更新 PRD 版本和相关 Eval：

1. 首案具体案情、指控罪名及是否只讨论定罪问题，还是包含教学性量刑分析；
2. 采用哪些现行实体法、程序法、司法解释和证据规范作为首案法源；
3. 哪些现实庭审环节被省略或简化，以及如何向用户标注；
4. 谁负责法律审核，其审核意见和版本如何归档；
5. 模型供应商、单场预算上限、最长回合和超时值；
6. Elasticsearch 8.19.10 的索引参数及法律文本 Embedding、重排模型的具体版本；
7. 训练数据、Eval 数据和试玩记录的保存期限及删除机制。

## 21. 后续版本候选

以下功能只有 MVP 达标后再评估：

- 更多经过审核的虚构案件；
- 用户自定义案卷导入；
- 教师创建和标注案例；
- 书记员摘要节点；
- 语音庭审；
- 多名真人联机；
- 专家证人；
- 更多程序性请求和教学规则；
- 陪审观点模拟；
- 不同模型与提示策略的 Eval 对比面板。
